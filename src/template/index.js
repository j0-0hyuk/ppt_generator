import { defaultTemplate } from "./default";
import { darkTemplate } from "./dark";
import { blueTemplate } from "./blue";

// 템플릿 객체로 관리
export const templates = {
  default: defaultTemplate,
  dark: darkTemplate,
  blue: blueTemplate,
};
